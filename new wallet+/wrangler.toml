# Wallet+ - Cloudflare Pages configuration (STATIC FALLBACK ONLY)
#
# IMPORTANT: This is a Python Flask application.
# Cloudflare Pages can only serve the static files in the 'public' folder.
# The real backend (login, dashboard, database, Send/Swap) will NOT work.
#
# For a working version use Render or Railway (see DEPLOYMENT.md).

name = "wallet-plus"
compatibility_date = "2024-01-01"

[site]
bucket = "./public"

# Recommended Cloudflare Pages settings:
#   Build command:        npm run build
#   Build output directory: public
#   Root directory:         (leave empty or .)
#
# This will only show a placeholder page explaining the limitation.
